import GodViewPanel from './GodViewPanel';

type PixelOfficePanelProps = {
  embedded?: boolean;
  minimal?: boolean;
};

export default function PixelOfficePanel(_props: PixelOfficePanelProps) {
  return <GodViewPanel />;
}
